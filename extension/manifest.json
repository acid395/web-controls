{
  "manifest_version": 3,
  "name": "web-controls (proof of concept)",
  "version": "0.3.0",
  "description": "Calls web-controls functions on a live page from an extension popup. USGS gets its own manifest; any other site uses the zero-manifest GENERIC fallback, enabled per-site at runtime from the popup rather than hardcoded here.",
  "permissions": ["scripting", "activeTab"],
  "host_permissions": [
    "https://waterdata.usgs.gov/*",
    "https://dashboard.waterdata.usgs.gov/*",
    "https://mywaterway.epa.gov/*",
    "https://www.drought.gov/*"
  ],
  "optional_host_permissions": ["https://*/*", "http://*/*"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup/popup.html"
  }
}
