{
  "manifest_version": 3,
  "name": "web-controls (proof of concept)",
  "version": "0.2.0",
  "description": "Calls web-controls functions on a live page from an extension popup. USGS gets its own manifest; other sites use the zero-manifest GENERIC fallback. Proves the popup -> background -> content script -> page bridge round-trips, on more than one site, before any LLM is involved.",
  "permissions": ["scripting", "activeTab"],
  "host_permissions": [
    "https://waterdata.usgs.gov/*",
    "https://dashboard.waterdata.usgs.gov/*",
    "https://mywaterway.epa.gov/*",
    "https://www.drought.gov/*"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup/popup.html"
  }
}
